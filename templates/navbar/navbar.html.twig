<nav class="navbar navbar-expand-lg navbar-light">
  <div class="container-fluid">
    <a class="navbar-brand text-white" href="{{ path('home')}}">Park'n drive</a>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <ul class="navbar-nav mb-2 mb-lg-0">
        {# <li class="nav-item">
          <a class="nav-link" href="#">Cars</a>
        </li> #}

        <li class="nav-item dropdown">
        {% if app.user %}
         <a class="nav-link dropdown-toggle text-white" href="#" id="navbarDropdown" role="button" data-bs-toggle="dropdown" aria-expanded="false">
            Hello, {{app.user.firstname}}
          </a>
         <ul class="dropdown-menu" aria-labelledby="navbarDropdown">
            <li><a class="dropdown-item" href="{{ path('user_default') }}">Add Car</a></li>
            <li><a class="dropdown-item" href="{{ path('user_showInfo') }}">Edit My Infos</a></li>
            {% if is_granted('ROLE_ADMIN') %}
                <a class="dropdown-item text-warning" href="{{ path('home_admin') }}">Administration</a>
            {% endif %}
            {% if is_granted('ROLE_DRIVER') %}
                <a class="dropdown-item text-success" href="{{ path('driver') }}">Driver</a>
            {% endif %}

            <li><hr class="dropdown-divider"></li>
            <li><a class="dropdown-item text-danger" href="{{ path('app_logout') }}">Logout</a></li>
          </ul>
         {% else %}
                <a aria-expanded="true" aria-haspopup="true" class="nav-link dropdown-toggle text-white" data-toggle="dropdown" href="#" role="button">
                    Login / registration</a>
                <div class="dropdown-menu" style="position: absolute; transform: translate3d(0px, 37px, 0px); top: 0px; left: 0px; will-change: transform;" x-placement="bottom-start">
                    <a class="dropdown-item" href="{{ path('app_login') }}">Login</a>
                    <a class="dropdown-item" href="{{ path('app_register') }}">Registration</a>
                </div>
        {% endif %}
        </li>
      </ul>
    </div>
  </div>
</nav>

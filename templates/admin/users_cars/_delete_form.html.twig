<form method="post" action="{{ path('users_cars_delete', {'id': users_car.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ users_car.id) }}">
    <button class="btn">Delete</button>
</form>
